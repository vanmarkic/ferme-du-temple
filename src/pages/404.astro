---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getEntry } from 'astro:content';

const notFoundEntry = await getEntry('notFound', 'not-found');
const content = notFoundEntry?.data;
---

<BaseLayout title="Page Non Trouvée | Ferme du Temple">
  <div class="min-h-screen flex items-center justify-center bg-background">
    <div class="text-center px-4">
      <h1 class="text-6xl font-display font-bold text-rich-black mb-4">{content?.title}</h1>
      <p class="text-xl text-rich-black/70 mb-8">{content?.message}</p>
      <a
        href="/"
        class="inline-block px-8 py-3 bg-magenta text-white font-medium hover:bg-magenta/90 transition-colors"
      >
        {content?.backButtonLabel}
      </a>
    </div>
  </div>
</BaseLayout>
