---
// Credit Castor is now a separate app - redirect to it
// In production, Vercel rewrite handles /admin/credit/* -> credit.lafermedutemple.be
// In dev, redirect to the credit app's dev server
return Astro.redirect(import.meta.env.DEV
  ? 'http://localhost:4322/admin/credit'
  : '/admin/credit');
---
